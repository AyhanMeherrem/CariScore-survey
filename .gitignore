# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Temporary files
*.tmp
*.temp

# Node modules (if any)
node_modules/
dist/

# Python cache (if any)
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Local development
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Backup files
*.bak
*.backup

# Survey data (contains participant responses)
survey_data/

# Legacy raw dataset / generation-pipeline files, not used by the SurveyJS
# app (which only reads from CaricatureImages/). Kept locally, excluded
# from git so the repo doesn't balloon to ~1GB.
OriginalImages/
pulid_outputs.zip
pulid_outputs/
instantid.zip
instantid/
faces.rar
faces/
temp/
jfif/
jfif2png.py
img2folder.py
instant.py
loracari.py
prompts.txt
id.png

# Claude Code local session config (may contain endpoint URLs used during
# testing, e.g. the Google Apps Script URL — not meant to be public).
.claude/